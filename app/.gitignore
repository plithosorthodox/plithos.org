# Generated on the build machine, not kept in the tree.
node_modules/
android/
www/
*.apk
*.aab

# vendor/ IS kept: the fonts there are gathered once (tools/vendor_fonts.py)
# and carried with the app so it needs no connection to display them.
